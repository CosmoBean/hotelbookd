version: '3.8'

services:
  mongo:
    image: mongo:7.0.2
    container_name: "hotelbookd-mongo"
    ports:
      - "${MONGO_PORT}:27017"
    volumes:
      - hotelbookd-mongo:/data/db

volumes:
  hotelbookd-mongo:
